# Production Environment
# For unified IIS deployment, DO NOT set NEXT_PUBLIC_API_URL
# The frontend will use relative URL '/api' which is handled by the same server

# Feature Flags
NEXT_PUBLIC_ENABLE_DARK_MODE=true
